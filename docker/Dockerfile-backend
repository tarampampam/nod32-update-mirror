FROM alpine:latest

ADD ./nod32-mirror ./crontab.conf /backend/

RUN apk --no-cache --update add bash unrar curl\
 && chmod +x /backend/nod32-mirror.sh /backend/include/*.sh\
 && crontab ./backend/crontab.conf

CMD ["/usr/sbin/crond", "-f", "-d", "0"]
